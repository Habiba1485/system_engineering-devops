#!/usr/bin/env bash
# This script lists all non-hidden files and prints only the part after the first dash.

for file in *; do
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
